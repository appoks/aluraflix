<section class="mb-8"> 
  <p id="notice"><%= notice %></p>

  <div class="flex justify-between items-end">
    <h1 class="text-xl mt-6 mb-2">Videos atualmente cadastrados:</h1> 
    <p class="bg-green-600 border rounded text-white font-bold py-2 px-3 mr-5 md:mr-0"> <%= link_to 'Adicionar Vídeo...', new_video_path %> </p>
  </div>


  <section class="grid grid-cols-3">
  <% @videos.each do |video| %>
    <%= render partial: "partials/admin/video_card", locals: {video: video} %>
  <% end %>
  </section>

  <br>
<!-- 
  <p class="bg-green-600 border rounded text-center text-white font-bold py-5 px-5 mx-5 md:mx-0"> <%= link_to 'Adicionar Vídeo...', new_video_path %> </p>
  -->
</section>
